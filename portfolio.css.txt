<div data-symbol="{{ symbol }}">
    <strong><a href="#" class="sse-display-stock" data-symbol="{{ symbol }}">{{ name }}</a></strong>,
    Quantity:
    <span class="sse-portfolio-quantity">{{ quantity }}</span>,
    Paid:
    <span class="sse-portfolio-ask">
        {{ ask }}
    </span>
</div>